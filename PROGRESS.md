# Progress

Status legend: 🕒 Planned · 🏗 In Progress · ✅ Done

## Features

| ID   | Feature                                          | Status |
| ---- | ------------------------------------------------ | ------ |
| F-01 | Colima VM resilience (self-healing LaunchDaemon) | ✅     |
| F-02 | Hermes gateway as a user-scoped host service     | ✅     |
| F-03 | Hermes dashboard as a user-scoped host service   | ✅     |

## Recent Agent Activity Log

> **Hephaestus (2026-07-07):** Recovered the crash-looping Colima VM under `apfelmus` (force-cleared a Broken Lima instance, re-bootstrapped the daemon); documented root cause in `colima-diagnosis.md`.
> **Hephaestus (2026-07-07):** Implemented F-01 — added `bin/colima-supervisor` (self-healing loop), switched the LaunchDaemon to run it with `ThrottleInterval=30`, and wired provisioning in `tasks/colima.yml`. Validated: shfmt/shellcheck clean, playbook syntax-check + plist render OK.
> **Hephaestus (2026-07-07):** Deployed F-01 to this host — installed the supervisor, swapped the LaunchDaemon. The daemon swap bounced the VM (~15s, hermes-agent restarted), which exercised the heal path for real: the supervisor detected the VM down, cleared stale state, and restarted it. Steady state healthy (colima Running, hermes-agent Up, daemon runs=1 never-exited).
> **Hephaestus (2026-07-07):** Validated via `ansible-playbook main.yml --tags colima`. Run 1 reported the plist `changed` (Ansible's template adds a trailing newline the manual render lacked) → re-bootstrapped and bounced the VM once more; supervisor recovered it. Run 2 was fully idempotent (`ok=12 changed=0 failed=0`, bootout/bootstrap correctly skipped, no bounce) — provisioning logic and change-detection conditionals confirmed.
> **Hephaestus (2026-08-25):** Implemented F-02 — added `tasks/hermes.yml` and `templates/hermes-gateway.plist.j2`, `hermes_*` vars in `default.config.yml`, wired `--tags hermes` into `main.yml`. The gateway now runs `hermes gateway run` in the foreground under a `de.contentreich.hermes-gateway` LaunchAgent (`KeepAlive=true`) with `HERMES_HOME=/Users/apfelmus/hermes-docker-daten`, deliberately avoiding hermes' own `ai.hermes.gateway` label.
> **Hephaestus (2026-08-25):** Cut over to the host service and removed `svc/hermes-agent/`. Stopped the container (up 2 weeks), merged its 8 credentials into `$HERMES_HOME/.env` (no key collisions — the file held only non-secret settings), cleared the gateway lock. Verified: `state = running`, `runs = 1`, never exited; API 401 without key / 200 with the migrated key; 21 open handles on `hermes-docker-daten` and 0 on `~/.hermes`; `kill -9` relaunched with `runs` 1→2; re-run idempotent (`changed=0`). Colima and the `ki-nginx`/`wp-kikreativ`/`ki-mariadb` stack untouched. Backups: `config.yaml.bak-precutover-*`, `.env.bak-precutover-*`.
> **Hephaestus (2026-08-25):** Hit the upstream launchctl exit-5 race (#11323, #42524) live — a plist change booted the agent out, the immediate bootstrap failed with `Bootstrap failed: 5: Input/output error`, and the gateway went down until re-bootstrapped. Hardened `tasks/hermes.yml`: after bootout it polls until the label leaves the GUI domain (`until`/`retries: 10`), and the bootstrap itself retries 5×. Re-tested by perturbing the plist — the wait fired one retry, bootstrap then succeeded first try.
> **Hephaestus (2026-08-25):** Confirmed boot survival (F-02 R7). A LaunchAgent does not exist before login, but this host auto-logs-in — `autoLoginUser = apfelmus`, `/etc/kcpassword` present, FileVault Off — so `gui/501` comes up unattended, launchd scans `~/Library/LaunchAgents`, the label is absent from `launchctl print-disabled gui/501`, and `RunAtLoad=true` starts the gateway. Recorded in the PRD that R7 now rests on auto-login staying enabled and FileVault staying off; enabling FileVault would silently stop the gateway returning after a reboot, and would force a LaunchDaemon conversion. Chain verified setting-by-setting, not by an actual reboot.
> **Hephaestus (2026-08-25):** Dropped the dashboard (port 9119) from scope. It was a separate s6-supervised process in the container image — `gateway run` never started it — and upstream documents it as an optional management surface that consumes the gateway's `/api/status` while nothing consumes it. Confirmed the gateway runs healthy without it and the desktop app holds no TCP connections to it. `--insecure` is also a no-op since the June 2026 hardening. PRD R3 and the acceptance criteria updated accordingly.
> **Hephaestus (2026-08-25):** Implemented F-02 — added `tasks/hermes.yml` and `templates/hermes-gateway.plist.j2`, `hermes_*` vars in `default.config.yml`, wired `--tags hermes` into `main.yml`. Runs `hermes gateway run` in the foreground under a `de.contentreich.hermes-gateway` LaunchAgent with `KeepAlive=true`, keeping `HERMES_HOME=/Users/apfelmus/hermes-docker-daten`; hermes' own `ai.hermes.gateway` agent is booted out and removed. Validated: `--syntax-check` OK, template renders and passes `plutil -lint`, `--check --tags hermes` clean (`ok=8 changed=1`, only the new plist). Confirmed `gui/501` is active and neither label is loaded. Note `make preflight` fails at `shell-test` on a pre-existing issue — `tests/` holds no `.bats` files, so `bats tests/` errors on `master` too. Cutover (compose down, secret merge into `$HERMES_HOME/.env`, stale lock removal, `svc/` deletion) not yet performed.
> **Hephaestus (2026-08-25):** Added a `justfile` (first one in the repo) with `hermes-update`, plus `hermes-status`, `hermes-check` and `hermes-restart`. `hermes-update` boots the LaunchAgent out, waits for the label to leave `gui/<uid>`, runs `hermes update` with `HERMES_HOME` exported (it is unset in apfelmus' login shell, so a bare update would back up and migrate against `~/.hermes`), then re-bootstraps with the exit-5 retry; a trap restarts the gateway on the old code if the update fails. Needed because `hermes update` only restarts the service when `ai.hermes.gateway.plist` exists (`hermes_cli/update_cmd.py:6518`), and F-02 deletes it. Validated: `just --fmt --check` clean, recipe body passes `bash -n` and `shellcheck`, both success and failure paths exercised against a stubbed `launchctl`. `hermes-status`/`hermes-check` run clean against the live gateway; `hermes-update` itself not yet run — `--check` reports the checkout is 1397 commits behind `origin/main`.
> **Hephaestus (2026-08-26):** Fixed repo/host drift in `hermes_home`. The data directory had been renamed on the host (`hermes-docker-daten` → `hermes-daten`, old tree archived as `hermes-docker-daten-260617.tar.gz`) and the live plist hand-edited to match, but `default.config.yml` and the `justfile` still named the old path — a provisioning run would have repointed the gateway at a nonexistent `HERMES_HOME` and bounced it. Corrected both; the gateway plist now renders byte-identical to the deployed one (`ok`, not `changed`), confirming the fix.
> **Hephaestus (2026-08-26):** Implemented F-03 — the dashboard is back, reversing the F-02 decision to drop it, because the Hermes desktop app requires the endpoint on 9119. Added `templates/hermes-dashboard.plist.j2` and `hermes_dashboard_*` vars; extracted F-02's hardened render/probe/bootout/wait/bootstrap sequence into `tasks/hermes-launchagent.yml` so both agents share it verbatim, with `tasks/hermes.yml` now looping over them. Two corrections to F-02's analysis: `--skip-build` serves the prebuilt SPA at `hermes_cli/web_dist`, so the predicted one-time `npm run build` is unnecessary as of v0.20.5; and a non-loopback bind is not merely insecure but fatal — `web_server.start_server` raises `SystemExit` with no registered auth provider and the interactive setup is TTY-only, so under `KeepAlive` it would crash-loop. Bound `127.0.0.1` accordingly; the desktop app is on this host.
> **Hephaestus (2026-08-26):** Deployed and verified F-03. `state = running`, `runs = 1`, never exited; `curl 127.0.0.1:9119` returns the SPA (200, 1720 B); `HERMES_DASHBOARD_READY port=9119` and the `--skip-build` line in `dashboard.log` with no build output; 3 open handles on `hermes-daten` and 0 on the `~/.hermes` state tree; `plutil -lint` OK. `kill -9` relaunched with `runs` 1→2. Re-run idempotent (`ok=15 changed=0 failed=0`). The F-02 gateway was untouched throughout — pid 6221, `runs = 3`, unchanged across both runs. Note the desktop app still holds the ephemeral backend it spawned before the agent existed (`serve --host 127.0.0.1 --port 0`, pid 6166); it should attach to 9119 on its next restart. `make ansible-preflight` clean; `make preflight` still fails at `shell-test` on the pre-existing empty-`tests/` issue (no `.bats` files, fails on `master` too).
> **Hephaestus (2026-08-26):** Extended the `justfile` for the second agent: `hermes-status` reports both labels, `hermes-dashboard-restart` added, and `hermes-update` now boots **both** agents out before updating — `hermes update` runs `_kill_stale_dashboard_processes` to stop a new JS bundle talking to an old in-memory backend, and under `KeepAlive` that kill would just respawn the dashboard onto half-updated files. Validated: `just --fmt --check` clean, recipe body passes `bash -n` and `shellcheck`, `just hermes-status` correct against both live agents.
